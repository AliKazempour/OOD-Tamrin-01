# OOD-Tamrin-01

# 📘 تمرین شماره ۱ – مروری بر اصول شی‌گرایی

## سیستم رزرو هتل – README

این مخزن شامل کدهای تمرین شمارهٔ ۱ درس طراحی شی‌گرای سیستم‌ها است.
مطابق داکیومنت تمرین، سیستم رزرو هتل در پنج گام توسعه، تحلیل و بازطراحی شده است.

---

# 🧩 گام اول – افزودن دو قابلیت جدید

در این مرحله، یک نسخهٔ کپی از پروژه در پوشه‌ی OOD-without-01-Step ایجاد شد و دو قابلیت خواسته‌شده به برنامه افزوده شد.

---

## ✔️ قابلیت اول: ارسال پیامک (SMS)

پیش از این، پیام تأیید فقط از طریق ایمیل ارسال می‌شد. در این گام:

* کلاس جدید `SmsSender` ایجاد شد.
* در `ReservationService` یک case برای SMS اضافه شد.
* از مقدار `customer.mobile` برای ارسال پیامک استفاده شد.

### تغییرات اعمال‌شده

| کلاس                 | توضیح تغییر                       |
| -------------------- | --------------------------------- |
| SmsSender          | ساخت کلاس جدید با متد sendSms() |
| ReservationService | افزودن case برای Notifier.SMS   |
| Notifier           | افزودن مقدار جدید SMS در enum     |

---

## ✔️ قابلیت دوم: پرداخت حضوری (On-site Payment)

در نسخهٔ اولیه فقط پرداخت‌های CARD`، `CASH و PAYPAL پشتیبانی می‌شد.

### تغییرات اعمال‌شده

* افزودن متد جدید onSitePayment() به `PaymentProcessor`
* افزودن case جدید در ReservationService برای روش پرداخت ONSITE

### جدول تغییرات

| کلاس                 | توضیح تغییر                              |
| -------------------- | ---------------------------------------- |
| PaymentProcessor   | افزودن متد onSitePayment()             |
| ReservationService | افزودن case برای PaymentMethods.ONSITE |

---

# 🧠 گام دوم – تحلیل اصول شی‌گرایی

در این گام نسخهٔ اولیهٔ سیستم از نظر اصول SOLID، PLK و CRP تحلیل شد.

---

## 🔹 اصل SRP

| وضعیت     | توضیح                                                                    |
| --------- | ------------------------------------------------------------------------ |
| رعایت‌شده | کلاس‌هایی مثل Customer و Room فقط وظیفه ذخیره‌سازی داده دارند.       |
| نقض‌شده   | ReservationService مسئول چند کار بوده: پرداخت، ارسال پیام، ایجاد رزرو. |

---

## 🔹 اصل OCP

| وضعیت     | توضیح                                                                       |
| --------- | --------------------------------------------------------------------------- |
| رعایت‌شده | متدهای پرداخت از هم جدا بودند.                                              |
| نقض‌شده   | برای اضافه‌کردن روش جدید، باید switch در ReservationService تغییر می‌کرد. |

---

## 🔹 اصل LSP

| وضعیت     | توضیح                                                   |
| --------- | ------------------------------------------------------- |
| رعایت‌شده | رفتار EmailSender مطابق انتظار interface پیام بود.    |
| نقض‌شده ۱ | طراحی interface پیام فقط برای ایمیل مناسب بود.          |
| نقض‌شده ۲ | اضافه‌کردن فرمت‌های پیام جدید ساده و جایگزین‌پذیر نبود. |

---

## 🔹 اصل ISP

| وضعیت     | توضیح                                                            |
| --------- | ---------------------------------------------------------------- |
| رعایت‌شده | مدل‌ها مجبور به پیاده‌سازی interfaceهای سنگین نبودند.            |
| نقض‌شده   | interface پیام فقط ارسال ایمیل را پوشش می‌داد و قابل توسعه نبود. |

---

## 🔹 اصل DIP

| وضعیت     | توضیح                                                                                   |
| --------- | --------------------------------------------------------------------------------------- |
| رعایت‌شده | استفاده از enum برای محدودکردن روش‌های پرداخت.                                          |
| نقض‌شده   | وابستگی مستقیم ReservationService به EmailSender, SmsSender و PaymentProcessor. |

---

## 🔹 اصل PLK (قانون دمتر)

| وضعیت     | توضیح                                                               |
| --------- | ------------------------------------------------------------------- |
| رعایت‌شده | برخی دسترسی‌ها ساختار ساده داشتند.                                  |
| نقض‌شده   | دسترسی‌های زنجیره‌ای مانند res.customer.email نقض قانون دمتر بود. |

---

## 🔹 اصل CRP

| وضعیت     | توضیح                                                    |
| --------- | -------------------------------------------------------- |
| رعایت‌شده | استفاده از Composition در ReservationService.          |
| نقض‌شده   | برای افزودن رفتار جدید، نیازمند تغییر کلاس‌های اصلی بود. |

---

# 🔧 گام سوم – اصلاح موارد نقض اصول شی‌گرایی

نسخهٔ جدیدی با نام OOD-with-02-Step ساخته شد و بازطراحی اساسی انجام گرفت.

---

## ✔️ اصلاح SRP

* تعریف `PaymentStrategy` برای انواع پرداخت
* ایجاد کلاس‌های مستقل:

  * CardPayment
  * CashPayment
  * PaypalPayment
  * OnSitePayment
* تعریف interface `NotificationSender`
* ساخت کلاس‌های EmailSender و SmsSender
* ساده‌سازی ReservationService برای ایفای نقش هماهنگ‌کننده

---

## ✔️ اصلاح OCP

* حذف switch-case در پرداخت و نوتیفیکیشن
* انتخاب استراتژی پرداخت با:

PaymentStrategy strategy = paymentProcessor.getPaymentStrategy(method);

* انتخاب نوتیفیکیشن با استفاده از Factory:

NotificationSender sender = notificationFactory.create(notifier);

---

## ✔️ اصلاح DIP

* تزریق وابستگی در سازندهٔ ReservationService
* وابستگی به abstraction به‌جای کلاس‌های concrete
* حذف وابستگی مستقیم به EmailSender, SmsSender, PaymentProcessor

---

## ✔️ اصلاح PLK

افزودن متدهای کمکی در Reservation:

* getCustomerName()
* getCustomerEmail()
* getCustomerMobile()
* getContactByNotifier()

و حذف دسترسی‌های زنجیره‌ای قبلی.

---

## ✔️ اصلاح ISP و CRP

* کوچک‌سازی interfaceها
* استفاده از Composition برای رفتارهای قابل‌توسعه
* افزودن قابلیت‌های جدید بدون نیاز به تغییر کلاس‌های اصلی

---

# 📊 گام چهارم – ارزیابی و مقایسه

## 🔚 قبل از بازطراحی

برای افزودن SMS و پرداخت حضوری نیاز به ۴–۵ نقطه تغییر بود:

* تغییر در ReservationService
* تغییر در PaymentProcessor
* افزوده‌شدن کلاس جدید
* تغییر در logicهای ایمیل

## 🔚 بعد از بازطراحی اصولی

فقط ۲ تغییر کافی بود:

* ایجاد کلاس جدید PaymentStrategy یا NotificationSender
* یک تغییر جزئی در Factory

✨ بدون هیچ تغییری در ReservationService!

---

# 🧠 گام پنجم – نتیجه‌گیری

این پروژه نشان داد رعایت اصول شی‌گرایی منجر به:

* توسعه‌پذیری بیشتر
* خوانایی بالاتر
* تست‌پذیری ساده‌تر
* نگه‌داری کم‌هزینه‌تر

می‌شود.

نسخهٔ بازطراحی‌شدهٔ سیستم:

* ماژولار و انعطاف‌پذیر است
* افزودن قابلیت‌های جدید بدون تغییر در کلاس‌های اصلی امکان‌پذیر است
* کلاس‌ها تک‌وظیفه‌ای هستند
* وابستگی‌ها کنترل‌شده و استانداردند
* قانون دمتر به‌خوبی رعایت شده است

---

# 🤖 استفادهٔ محدود و مسئولانه از هوش مصنوعی

در بخش‌هایی از این پروژه از AI فقط به‌عنوان دستیار استفاده شد، شامل:

* کمک در نصب JDK و ساخت پروژه در VS Code
* بررسی برخی سینتکس‌ها و رفع خطاهای اولیه
* ارائهٔ پیشنهادهای کلی در ساختار شی‌گرا

اما تمام موارد زیر توسط خودمان انجام شده است:

* تحلیل‌ها
* پیاده‌سازی نهایی
* بازطراحی گام سوم
* نگارش گزارش‌ها

AI تنها نقش کمکی داشته و بخش اصلی کار انسانی بوده است.
