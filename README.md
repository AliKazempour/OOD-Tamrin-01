# OOD-Tamrin-01

# 📘 تمرین شماره ۱ – مروری بر اصول شی‌گرایی

## سیستم رزرو هتل – README

این مخزن شامل کدهای تمرین شماره ۱ درس طراحی شی‌گرای سیستم‌ها است.
مطابق داکیومنت تمرین، سیستم رزرو هتل در پنج گام توسعه، تحلیل و بازطراحی شده است.

---

# 🧩 گام اول – افزودن دو قابلیت جدید

در این گام، یک نسخهٔ کپی از پروژه در پوشه‌ی OOD-without-01-Step ساخته شد و دو قابلیت طبق صورت‌تمرین به برنامه اضافه گردید.

---

## ✔️ قابلیت اول: ارسال پیامک (SMS)

پیش از این، پیام تأیید فقط از طریق ایمیل ارسال می‌شد. تغییرات انجام‌شده:

* ایجاد کلاس جدید SmsSender
* افزودن case مربوط به SMS در ReservationService
* استفاده از customer.mobile برای ارسال پیامک

تغییرات:

| کلاس                 | توضیح تغییر                       |
| -------------------- | --------------------------------- |
| SmsSender          | ساخت کلاس جدید با متد sendSms() |
| ReservationService | افزودن case برای Notifier.SMS   |
| Notifier           | استفاده از مقدار SMS در enum    |

---

## ✔️ قابلیت دوم: پرداخت حضوری (On-site Payment)

در نسخهٔ اولیه فقط پرداخت‌های CARD، CASH و PAYPAL وجود داشت.

تغییرات انجام‌شده:

* افزودن متد onSitePayment() به PaymentProcessor
* افزودن case جدید در بخش پرداخت داخل ReservationService

تغییرات:

| کلاس                 | توضیح تغییر                                  |
| -------------------- | -------------------------------------------- |
| PaymentProcessor   | افزودن متد onSitePayment()                 |
| ReservationService | افزودن case مربوط به PaymentMethods.ONSITE |

---

# 🧠 گام دوم – تحلیل اصول شی‌گرایی

در این بخش، نسخهٔ اولیهٔ پروژه از نظر اصول SOLID، PLK و CRP تحلیل شد. نتایج در جدول زیر آمده است:

---

## اصل SRP

| وضعیت            | توضیح                                                                                |
| ---------------- | ------------------------------------------------------------------------------------ |
| مورد برقراری | کلاس‌های مدل مانند Customer و Room فقط وظیفهٔ نگه‌داری داده را دارند.            |
| مورد نقض     | ReservationService هم پرداخت انجام می‌دهد، هم پیام ارسال می‌کند و هم رزرو می‌سازد. |

---

## اصل OCP

| وضعیت            | توضیح                                                                     |
| ---------------- | ------------------------------------------------------------------------- |
| مورد برقراری | متدهای پرداخت مجزا بودند.                                                 |
| مورد نقض     | با اضافه‌کردن روش جدید، باید switch در ReservationService تغییر می‌کرد. |

---

## اصل LSP

| وضعیت            | توضیح                                          |
| ---------------- | ---------------------------------------------- |
| مورد برقراری | رفتار EmailSender مطابق interface پیام است.  |
| مورد نقض ۱   | طراحی interface پیام فقط برای ایمیل مناسب بود. |
| مورد نقض ۲   | انواع پیام جدید به‌راحتی قابل جایگزینی نبودند. |

---

## اصل ISP

| وضعیت            | توضیح                                                            |
| ---------------- | ---------------------------------------------------------------- |
| مورد برقراری | مدل‌ها مجبور به پیاده‌سازی interfaceهای بزرگ نبودند.             |
| مورد نقض     | interface پیام فقط ارسال ایمیل را پوشش می‌داد و قابل گسترش نبود. |

---

## اصل DIP

| وضعیت            | توضیح                                                                                        |
| ---------------- | -------------------------------------------------------------------------------------------- |
| مورد برقراری | استفاده از enum برای محدودکردن انواع پرداخت.                                                 |
| مورد نقض     | وابستگی مستقیم ReservationService به EmailSender`، `SmsSender و متدهای PaymentProcessor. |

---

## اصل PLK

| وضعیت            | توضیح                                                               |
| ---------------- | ------------------------------------------------------------------- |
| مورد برقراری | بعضی دسترسی‌ها ساده بود.                                            |
| مورد نقض     | دسترسی‌های زنجیره‌ای مانند res.customer.email نقض قانون دمتر بود. |

---

## اصل CRP

| وضعیت            | توضیح                                                   |
| ---------------- | ------------------------------------------------------- |
| مورد برقراری | استفاده از Composition در ReservationService.         |
| مورد نقض     | برای افزودن رفتار جدید نیاز به تغییر کلاس‌های اصلی بود. |

---

# 🔧 گام سوم – اصلاح موارد نقض اصول شی‌گرایی

در این گام، نسخه‌ای با عنوان OOD-with-02-Step ایجاد و بازطراحی کامل انجام شد.
هدف: اصلاح نقض‌های گام دوم و ایجاد یک معماری استاندارد شی‌گرا.

---

## ✔️ اصلاح SRP

* تعریف interface PaymentStrategy
* ایجاد کلاس‌های مستقل:

  * CardPayment
  * CashPayment
  * PaypalPayment
  * OnSitePayment
* تعریف interface NotificationSender
* ساخت کلاس‌های EmailSender و SmsSender
* ساده‌سازی ReservationService برای عمل‌کردن فقط به‌عنوان هماهنگ‌کننده

---

## ✔️ اصلاح OCP

* حذف switch پرداخت و نوتیفیکیشن
* انتخاب استراتژی پرداخت با:

PaymentStrategy strategy = paymentProcessor.getPaymentStrategy(method);

* انتخاب نوتیفیکیشن با Factory:

NotificationSender sender = notificationFactory.create(notifier);

---

## ✔️ اصلاح DIP

* تزریق وابستگی‌ها در سازندهٔ ReservationService
* وابستگی به abstraction به جای کلاس‌های concrete
* حذف وابستگی مستقیم به EmailSender, SmsSender, PaymentProcessor

---

## ✔️ اصلاح PLK

* افزودن متدهای کمکی زیر در کلاس Reservation:

getCustomerName()
getCustomerEmail()
getCustomerMobile()
getContactByNotifier()

* حذف دسترسی‌های زنجیره‌ای مثل res.customer.email

---

## ✔️ اصلاح ISP و CRP

* کوچک‌سازی interfaceها
* استفاده از Composition برای رفتارهای جدید
* افزودن قابلیت جدید بدون تغییر در کلاس‌های اصلی

---

# 📊 گام چهارم – ارزیابی

این گام بررسی کرد که اگر اصول شی‌گرایی از ابتدا رعایت می‌شد:

> «برای افزودن SMS و پرداخت حضوری، چند تغییر کمتر لازم بود؟»

---

## 🔚 قبل از اصلاح (طراحی اولیه):

* ۴ تا ۵ نقطه تغییر ضروری بود:

  * تغییر در ReservationService
  * تغییر در PaymentProcessor
  * افزودن کلاس جدید
  * تغییر در logicهای ایمیل

## 🔚 بعد از اصلاح (طراحی اصولی):

* تنها ۲ کار لازم بود:

  * ایجاد کلاس جدید PaymentStrategy یا NotificationSender
  * یک تغییر کوچک در Factory

🍀 هیچ تغییری در ReservationService لازم نبود!

---

# 🧠 گام پنجم – نتیجه‌گیری (به‌همراه اشاره به استفادهٔ محدود از هوش مصنوعی)

این پروژه نشان داد که رعایت اصول شی‌گرایی چگونه می‌تواند تأثیر مستقیم بر:

* توسعه‌پذیری
* خوانایی
* تست‌پذیری
* نگه‌داری نرم‌افزار

داشته باشد.

سیستم رزرو هتل بازطراحی‌شده اکنون:

* ماژولار و انعطاف‌پذیر است
* افزودن قابلیت‌های جدید بدون تغییر در کلاس‌های اصلی انجام می‌شود
* هر کلاس فقط مسئول یک کار مشخص است
* وابستگی‌ها استاندارد و قابل کنترل هستند
* قانون دمتر رعایت شده است

---

## 🤖 استفادهٔ محدود و مسئولانه از هوش مصنوعی

در بخش‌هایی از پروژه، از هوش مصنوعی صرفاً به‌عنوان ابزار کمکی استفاده شده است، از جمله:

* راهنمایی در نصب JDK و ساخت پروژه در VS Code
* بررسی برخی سینتکس‌ها و رفع خطاهای اولیه
* ارائهٔ پیشنهادهای کلی برای ساختار شی‌گرایی

اما:

* کلیهٔ تحلیل‌ها
* پیاده‌سازی نهایی کد
* بازطراحی گام سوم
* و نوشتن گزارش‌ها

توسط خودم انجام شده و AI فقط نقش «دستیار فنی» داشته است.

